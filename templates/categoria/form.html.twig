{% extends 'base.html.twig' %}
{% block title %}{{titulo}}{% endblock %}
{% block body %}
<h1>{{titulo}}</h1>

<a href="{{ path('categoria_index') }}" class="btn btn-primary mb-4 mt-4">Categorias Cadastradas</a>

{{ form(form) }}

<p>{{mensagem}}</p>
{% endblock %}
