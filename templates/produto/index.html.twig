{% extends 'base.html.twig' %}
{% block title %}{{titulo}}{% endblock %}
{% block body %}
<center>
    <h1 class="p-3">{{titulo}}</h1>
</center>

<a href="{{ path('produto_adicionar') }}" class="btn btn-primary mb-3">+ Adicionar</a>
<table class="table table-dark table-striped table-bordered">
    <thead>
        <tr>
            <td>ID</td>
            <td>NOME</td>
            <td>VALOR</td>
            <td>CATEGORIA</td>
            <td>AÇÕES</td>
        </tr>
    </thead>
    <tbody>
        {% for produto in produtos %}
            <tr>
                <td>{{produto.id}}</td>
                <td>{{produto.nomeproduto}}</td>
                <td>R$ {{produto.valor}}</td>
                <td>{{produto.categoria.descricaocategoria}}</td>
                <td>
                    <a href="{{ path("produto_editar", {id: produto.id}) }}" class="btn btn-primary">Editar</a>
                    <a href="{{ path("produto_excluir", {id: produto.id}) }}" class="btn btn-primary">Excluir</a>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}
