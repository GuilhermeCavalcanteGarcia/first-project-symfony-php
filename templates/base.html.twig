<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
        
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {#
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
        #}

    </head>
    <body class="bg-warning"> 
        <nav class="navbar navbar-expand-lg navbar-light bg-dark p-4">
            <a class="navbar-brand text-primary alert-link" href="#">Cavalcante App !</a> 
            {% if is_granted('ROLE_USER') %}    
            <div class="collapse navbar-collapse d-flex justify-content-end" id="navbarNav">
              <ul class="navbar-nav">
                <li class="nav-item active">
                  <a class="nav-link text-primary alert-link" href="{{ path('categoria_index') }}">Categorias</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link text-primary alert-link" href="{{ path('produto_index') }}">Produtos</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link text-primary alert-link" href="{{ path('app_logout') }}">Sair</a>
                </li>
              </ul>
            </div>
            {% endif %}
          </nav>
        <div class="container text-primary">
            <div class="d-flex mt-3">
              {% if is_granted('ROLE_USER')%}
    
                    <span class="alert alert-warning alert-link">Bem vindo, {{ app.user.nome }}!</span>
  
              {% endif %}
            </div>
            {% block body %}{% endblock %}
        </div>
    </body>
</html>